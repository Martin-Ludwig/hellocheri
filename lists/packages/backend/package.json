{
  "name": "@lists/backend",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "test": "bun test",
    "container:build": "--ignorefile .containerignore"
  },
  "dependencies": {
    "@lists/shared": "workspace:*",
    "elysia": "latest"
  }
}
